export interface frontendLibOptions {
  clientLibrary?: string;
  resourcesPath?: string;
  documentRoot?: string;
  patchFile?: string;
  projectPath?: string;
  devCommand?: string;
  devUrl?: string;
  waitTimeout?: number;
}